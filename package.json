{
  "packageManager": "yarn@1.22.19",
  "scripts": {
    "start": "parcel serve src/index.html",
    "build": "rimraf dist && cross-env NODE_ENV=production parcel build src/index.html --public-url .",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "gh-pages": "^5.0.0",
    "parcel-bundler": "^1.12.5",
    "rimraf": "^4.1.2",
    "typescript": "^4.9.5"
  }
}
